<Window x:Class="DS4WinWPF.DS4Forms.ControllerRegisterOptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DS4WinWPF.DS4Forms"
        xmlns:lex="http://wpflocalizeextension.codeplex.com"
        lex:LocalizeDictionary.DesignCulture=""
        lex:ResxLocalizationProvider.DefaultAssembly="DS4Windows"
        lex:ResxLocalizationProvider.DefaultDictionary="Strings"
        mc:Ignorable="d"
        Title="{lex:Loc ControllerRegOptWin.WinTitle}" Height="450" Width="800"
        Style="{DynamicResource WindowStyle}" Closed="Window_Closed">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>
    <DockPanel x:Name="devOptionsDockPanel" Margin="4">
        <DockPanel>
            <StackPanel DockPanel.Dock="Top">
                <CheckBox Content="{lex:Loc ControllerSupportDS4}" IsChecked="{Binding DS4DeviceOpts.Enabled}" />
                <CheckBox Content="{lex:Loc ControllerSupportDualSense}" IsChecked="{Binding DSDeviceOpts.Enabled}" />
                <CheckBox Content="{lex:Loc ControllerSupportSwitchPro}" IsChecked="{Binding SwitchProDeviceOpts.Enabled}" />
                <CheckBox Content="{lex:Loc ControllerSupportJoyCon}" IsChecked="{Binding JoyConDeviceOpts.Enabled}" />
                <CheckBox Content="{lex:Loc ControllerSupportDS3}" IsChecked="{Binding DS3DeviceOpts.Enabled}" />
                <CheckBox Content="{lex:Loc ControllerSupportMoonlight}" IsChecked="{Binding UseMoonlight}" />
                <CheckBox Content="{lex:Loc AdvancedSupport}" IsChecked="{Binding UseAdvancedMoonlight}"
                          Visibility="{Binding Path=UseMoonlight, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="20 0 0 0"/>
                <TextBlock>
                    <Hyperlink NavigateUri="https://github.com/schmaldeo/DS4Windows/blob/moonlight/doc/moonlight_support.md"
                               RequestNavigate="Hyperlink_RequestNavigate">
                        About virtual controller support
                    </Hyperlink>
                </TextBlock>
            </StackPanel>

            <StackPanel DockPanel.Dock="Top" Margin="0,16,0,0">
                <CheckBox Content="{lex:Loc EnableVerboseLog}" IsChecked="{Binding VerboseLogMessages}" ToolTip="{lex:Loc EnableVerboseLogTip}"/>
            </StackPanel>
            
            <DockPanel DockPanel.Dock="Bottom" Margin="0,20,0,0">
                <Label Content="{lex:Loc DetectedControllers}" ContentStringFormat="{}{0}:" DockPanel.Dock="Top" FontWeight="Bold" />
                <ListBox x:Name="controllerListBox" ItemsSource="{Binding CurrentInputDevices}" SelectedIndex="{Binding ControllerSelectedIndex}"
                      Margin="0,8,0,0">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding IdText}" />
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
            
        </DockPanel>

        <TabControl x:Name="deviceSettingsTabControl" DockPanel.Dock="Right" SelectedIndex="{Binding CurrentTabSelectedIndex}" Margin="40,4,4,4">
            <TabItem Header="Blank" Visibility="Collapsed">
            </TabItem>

            <TabItem x:Name="ds4OptsTabItem" Header="DS4" Visibility="Collapsed">
                <StackPanel Visibility="{Binding Visible, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="4">
                    <CheckBox Content="{lex:Loc Copycat}" IsChecked="{Binding Options.IsCopyCat, FallbackValue='False'}" ToolTip="{lex:Loc CopycatTip}" />
                </StackPanel>
            </TabItem>
            
            <TabItem x:Name="dualSenseOptsTabItem" Header="DualSense" Visibility="Collapsed">
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel Visibility="{Binding Visible, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="4">
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <Label Content="{lex:Loc ControllerRegOptWin.PlayerLEDMode}" ContentStringFormat="{}{0}:" />
                        <ComboBox ItemsSource="{Binding Path=DsLEDModes}" DisplayMemberPath="DisplayName" SelectedValuePath="ChoiceValue" SelectedValue="{Binding Options.LedMode}" Margin="8,0,0,0" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <Label Content="{lex:Loc ControllerRegOptWin.MuteLEDMode}" ContentStringFormat="{}{0}:" />
                        <ComboBox ItemsSource="{Binding Path=DsMuteLEDModes}" DisplayMemberPath="DisplayName" SelectedValuePath="ChoiceValue" SelectedValue="{Binding Options.MuteLedMode}" Margin="8,0,0,0" />
                    </StackPanel>

                    <Separator Margin="0,12,0,4" />
                    <TextBlock Text="Bluetooth Haptic Feedback (audio-based)" FontWeight="Bold" Margin="0,4,0,0" />
                    <TextBlock TextWrapping="Wrap" Foreground="Gray" Margin="0,2,0,0"
                               Text="Streams haptic audio to the controller's actuators over Bluetooth. System Audio converts what you hear into haptics; Rumble To Haptics re-voices game rumble through the haptic actuators." />

                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <Label Content="Haptics mode:" />
                        <ComboBox ItemsSource="{Binding Path=DsHapticsModes}" DisplayMemberPath="DisplayName" SelectedValuePath="ChoiceValue" SelectedValue="{Binding Options.BTHapticsMode}" Margin="8,0,0,0" MinWidth="180" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <Label Content="Audio source:" />
                        <ComboBox ItemsSource="{Binding Path=HapticsAudioDevices}" DisplayMemberPath="DisplayName" SelectedValuePath="Id" SelectedValue="{Binding Options.BTHapticsAudioDeviceId}" Margin="8,0,0,0" MinWidth="260" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <Label Content="Intensity:" />
                        <Slider Value="{Binding Options.BTHapticsGain}" Minimum="0.5" Maximum="8" TickFrequency="0.5" IsSnapToTickEnabled="True" Width="180" Margin="8,4,0,0" VerticalAlignment="Center" />
                        <TextBlock Text="{Binding Options.BTHapticsGain}" Margin="8,4,0,0" VerticalAlignment="Center" MinWidth="32" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <Label Content="Low-pass filter (Hz):" ToolTip="Lower values = only deep bass becomes haptics; higher values = more texture from the audio." />
                        <Slider Value="{Binding Options.BTHapticsLowPassHz}" Minimum="80" Maximum="800" TickFrequency="10" IsSnapToTickEnabled="True" Width="180" Margin="8,4,0,0" VerticalAlignment="Center" />
                        <TextBlock Text="{Binding Options.BTHapticsLowPassHz}" Margin="8,4,0,0" VerticalAlignment="Center" MinWidth="32" />
                    </StackPanel>

                    <Separator Margin="0,12,0,4" />
                    <TextBlock Text="Bluetooth Audio (headphones / speaker)" FontWeight="Bold" Margin="0,4,0,0" />
                    <TextBlock TextWrapping="Wrap" Foreground="Gray" Margin="0,2,0,0"
                               Text="Streams the selected audio source to the controller's headphone jack or built-in speaker over Bluetooth — no USB cable needed." />

                    <CheckBox Content="Send audio to controller" IsChecked="{Binding Options.BTAudioEnabled}" Margin="0,8,0,0" />

                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <Label Content="Output:" />
                        <ComboBox ItemsSource="{Binding Path=DsAudioRoutes}" DisplayMemberPath="DisplayName" SelectedValuePath="ChoiceValue" SelectedValue="{Binding Options.BTAudioRoute}" Margin="8,0,0,0" MinWidth="220" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <Label Content="Volume:" />
                        <Slider Value="{Binding Options.BTAudioVolume}" Minimum="0" Maximum="100" TickFrequency="5" IsSnapToTickEnabled="True" Width="180" Margin="8,4,0,0" VerticalAlignment="Center" />
                        <TextBlock Text="{Binding Options.BTAudioVolume}" Margin="8,4,0,0" VerticalAlignment="Center" MinWidth="32" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <Label Content="Buffering:" ToolTip="Smooth = most stable, ~0.3-0.4 s behind. Low latency = ~0.1 s behind, but needs a clean Bluetooth link (interference causes dropouts). Applies to both audio and audio-based haptics." />
                        <ComboBox ItemsSource="{Binding Path=DsAudioLatencies}" DisplayMemberPath="DisplayName" SelectedValuePath="ChoiceValue" SelectedValue="{Binding Options.BTAudioLatency}" Margin="8,0,0,0" MinWidth="220" />
                    </StackPanel>

                    <TextBlock TextWrapping="Wrap" Foreground="Gray" Margin="0,6,0,0"
                               Text="Bluetooth connections only. Applies immediately. While streaming is active, standard rumble emulation is disabled (the controller cannot do both at once) — use a haptics mode that includes Rumble to keep feeling game rumble. Pick the audio source your game actually plays through. Protocol research credit: egormanga/SAxense and awalol/DS5Dongle." />

                    <GroupBox Header="Native passthrough (virtual wired DualSense)" Margin="0,16,0,4">
                        <StackPanel Margin="8">
                            <TextBlock TextWrapping="Wrap" Foreground="Gray"
                                       Text="Lets games use their native wired DualSense support while this controller remains connected over Bluetooth." />

                            <CheckBox Content="Relay native game speaker audio"
                                      IsChecked="{Binding Options.NativeModeSpeakerAudio}"
                                      IsEnabled="{Binding NativeModeSettingsEnabled}"
                                      Margin="0,8,0,0" />

                            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                <Label Content="Controller audio output:" />
                                <ComboBox ItemsSource="{Binding Path=DsAudioRoutes}"
                                          DisplayMemberPath="DisplayName"
                                          SelectedValuePath="ChoiceValue"
                                          SelectedValue="{Binding Options.NativeModeRoute}"
                                          IsEnabled="{Binding NativeModeSettingsEnabled}"
                                          Margin="8,0,0,0" MinWidth="220" />
                            </StackPanel>

                            <Button Content="{Binding NativeModeButtonText}"
                                    IsEnabled="{Binding NativeModeCanToggle}"
                                    Click="NativeModeButton_Click"
                                    HorizontalAlignment="Left" MinWidth="140"
                                    Margin="0,10,0,0" Padding="8,3" />

                            <Button Content="Set up native mode"
                                    IsEnabled="{Binding NativeModeSetupCanRun}"
                                    Click="NativeModeSetupButton_Click"
                                    HorizontalAlignment="Left" MinWidth="140"
                                    Margin="0,6,0,0" Padding="8,3" />

                            <TextBlock Text="{Binding NativeModeStatus}" TextWrapping="Wrap"
                                       Margin="0,8,0,0" />
                            <TextBlock Text="Setup creates a fixed elevated attach task after one UAC prompt. Changing UsbipExePath requires running setup again."
                                       TextWrapping="Wrap" Foreground="Gray" Margin="0,5,0,0" />
                        </StackPanel>
                    </GroupBox>
                </StackPanel>
                </ScrollViewer>
            </TabItem>

            <TabItem x:Name="switchProOptsTabItem" Header="Switch Pro" Visibility="Collapsed">
                <StackPanel Visibility="{Binding Visible, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="4">
                    <TextBlock Foreground="#FFDE4343" FontWeight="Bold">
                        <Run Text="*  -  " />
                        <Run Text="{lex:Loc ChangeOnNewDeviceConn}" />
                    </TextBlock>

                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <TextBlock Text="*" Foreground="Red" />
                        <CheckBox Content="{lex:Loc ControllerRegOptWin.EnableHomeLED}" IsChecked="{Binding Options.EnableHomeLED, FallbackValue='False'}" Margin="8,0,0,0" />
                    </StackPanel>
                </StackPanel>
            </TabItem>

            <TabItem x:Name="joyConOptsTabItem" Header="JoyCon" Visibility="Collapsed">
                <StackPanel Visibility="{Binding Visible, Converter={StaticResource BooleanToVisibilityConverter}}" Margin="4">
                    <TextBlock Foreground="#FFDE4343" FontWeight="Bold">
                        <Run Text="*  -  " />
                        <Run Text="{lex:Loc ChangeOnNewDeviceConn}" />
                    </TextBlock>

                    <GroupBox Header="JoyCon Global Options" Margin="0,8,0,0">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                <TextBlock Text="*" Foreground="Red" Margin="0,0,8,0" VerticalAlignment="Center" />
                                <Label Content="{lex:Loc ControllerRegOptWin.LinkMode}" ContentStringFormat="{}{0}:" />
                                <ComboBox ItemsSource="{Binding Path=LinkModes}" DisplayMemberPath="DisplayName" SelectedValuePath="ChoiceValue" SelectedValue="{Binding ParentOptions.LinkedMode}" Margin="8,0,0,0" />
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                <TextBlock Text="*" Foreground="Red" Margin="0,0,8,0" VerticalAlignment="Center" />
                                <Label Content="{lex:Loc ControllerRegOptWin.JoinedGyroProvider}" ContentStringFormat="{}{0}:" />
                                <ComboBox ItemsSource="{Binding Path=JoinGyroOptions}" DisplayMemberPath="DisplayName" SelectedValuePath="ChoiceValue" SelectedValue="{Binding ParentOptions.JoinGyroProv}" Margin="8,0,0,0" />
                            </StackPanel>
                        </StackPanel>
                    </GroupBox>

                    <StackPanel Orientation="Horizontal" Margin="0,16,0,0">
                        <TextBlock Text="*" Foreground="Red" Margin="0,0,8,0" VerticalAlignment="Center" />
                        <CheckBox Content="{lex:Loc ControllerRegOptWin.EnableHomeLED}" IsChecked="{Binding Options.EnableHomeLED, FallbackValue='False'}" />
                    </StackPanel>
                </StackPanel>
            </TabItem>
        </TabControl>
    </DockPanel>
</Window>
